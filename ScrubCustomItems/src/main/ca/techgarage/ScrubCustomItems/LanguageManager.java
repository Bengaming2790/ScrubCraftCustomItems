package main.ca.techgarage.ScrubCustomItems;
import org.bukkit.configuration.file.FileConfiguration;
import org.bukkit.configuration.file.YamlConfiguration;

import java.io.File;
import java.io.InputStream;
import java.io.InputStreamReader;
import java.nio.charset.StandardCharsets;

public class LanguageManager {
   /* private final Main plugin; // Your main plugin instance
    private FileConfiguration langConfig;

    public LanguageManager(Main plugin) {
        this.plugin = plugin;
    }
        private FileConfiguration messages;

        public void loadLanguage(String language) {
            File file = new File(plugin.getDataFolder(), "messages_" + language + ".yml");
            if (!file.exists()) {
                plugin.saveResource("messages_" + language + ".yml", false);  // Copy from resources if not found
            }
            this.messages = YamlConfiguration.loadConfiguration(file);  // Load the file from the data folder
        }

        public String getMessage(String key) {
            return messages.getString(key, "Message not found");
        } */
    }

